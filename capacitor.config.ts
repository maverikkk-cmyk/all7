import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.maverikkk.all7',
  appName: 'all7',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
